/* 
  variant - for different designs
  sizes - for different sized such as lg, md, sm
  children - rendering the text inside of the button
  color - different colors such as primary, secondary, transparent, success, error, warning
*/

const Button = () => {};

export default Button;
